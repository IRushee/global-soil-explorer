[project]
name = "global-soil-explorer-backend"
version = "0.1.0"
description = "Backend logic for Global Soil Explorer"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"backend" = "."}
packages = [
    "backend.domain",
    "backend.domain.value_objects",
    "backend.application",
    "backend.contracts",
    "backend.repository",
    "backend.spatial",
    "backend.processing",
    "backend.shared",
    "backend.config",
    "backend.interfaces",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "mypy",
]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "PL"]
ignore = []

[tool.ruff.lint.per-file-ignores]
"**/test_*.py" = ["PLR2004"]


[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
